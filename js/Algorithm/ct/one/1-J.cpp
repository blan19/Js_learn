#include <bits/stdc++.h>
using namespace std;
int test, input;
string s_1, s_2;
int main() {
  ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
  cin >> test;
  for(int i = 0; i < test; i++) {
    cout << "\n";
    cin >> input;
    for(int j = 0; j < input; j++) {
      cin >> s_1 >> s_2;
    }
  }
  return 0;
}

// 조합(combination)